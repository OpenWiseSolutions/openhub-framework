import { positiveColor, negativeColor } from '../../../styles/colors'

export default {
  main: {
    fontWeight: 800
  },
  up: {
    color: positiveColor
  },
  down: {
    color: negativeColor
  }
}
